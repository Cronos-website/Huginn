apiVersion: v1
kind: Service
metadata:
  name: huginn-hub
  namespace: huginn
  labels:
    app: huginn-hub
spec:
  selector:
    app: huginn-hub
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
